describe("The question list", () => {
    beforeEach(() => {
        console.log("pinguiLee")
    })
    beforeAll(() => {
        console.log("bulbassauro")
    })
    afterEach(() => {
        console.log("Cachorro ALADO")
    })
    afterAll(() => {
        console.log("Dagron Latindo")
    })
    
    it("Should display a list of items", () => {
        expect(40 + 2).toEqual(42)
    })
    it("Should display a list of items", () => {
        expect(40 + 2).toEqual(42)
    })
    it("Should display a list of items", () => {
        expect(40 + 2).toEqual(42)
    })
})